@using ShoesProject.Models
@using System.Globalization

<head>


        <link rel="stylesheet" href="~/Content/linearicons.css">
    <link rel="stylesheet" href="~/Content/font-awesome.min.css">
    <link rel="stylesheet" href="~/Content/themify-icons.css">
    <link rel="stylesheet" href="~/Content/bootstrap.css">
    <link rel="stylesheet" href="~/Content/owl.carousel.css">
    <link rel="stylesheet" href="~/Content/nice-select.css">
    <link rel="stylesheet" href="~/Content/nouislider.min.css">
    <link rel="stylesheet" href="~/Content/ion.rangeSlider.css" />
    <link rel="stylesheet" href="~/Content/ion.rangeSlider.skinFlat.css" />
    <link rel="stylesheet" href="~/Content/magnific-popup.css">
    <link rel="stylesheet" href="~/Content/main.css" />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
</head>
<!-- Start Header Area -->
<header class="header_area sticky-header" id="menu_load">
    <div class="main_menu">
        <nav class="navbar navbar-expand-lg navbar-light main_box">

            <div class="container">
                <!-- Brand and toggle get grouped for better mobile display -->
                <a class="navbar-brand logo_h" href="index.html"><img src="~/img/logo.png" alt=""></a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse offset" id="navbarSupportedContent">
                    @if (Session["acc"] == null)
                    {
                        <ul class="nav navbar-nav menu_nav ml-auto">
                            <li class="nav-item" id="homeMenu"><a class="nav-link" href="@Url.Action("Index", "Home")">@ShoesProject.Resources.Data.Home</a></li>
                            <li class="nav-item" id="productMenu"><a class="nav-link" href="@Url.Action("SanPham", "SanPham")">@ShoesProject.Resources.Data.Product</a></li>
                            <li class="nav-item" id="signinMenu"><a class="nav-link" href="/Login/SignIn">@ShoesProject.Resources.Data.Sign</a></li>
                            <li class="nav-item" id="signupMenu"><a class="nav-link" href="/Register/SignUp">@ShoesProject.Resources.Data.Signup</a></li>
                            <li class="nav-item">
                            </li>

                        </ul>
                        <ul class="nav navbar-nav">
                            @using (Html.BeginForm())
                            {
                                <select id="lang" name="lang" onchange="this.form.submit()">
                                    <option value="en-US" @(Culture != "vi-VN" ? "selected" : "")>English</option>
                                    <option value="vi-VN" @(Culture == "vi-VN" ? "selected" : "")>Tiếng Việt</option>
                                </select>
                            }

                        </ul>
                    }
                    else
                    {
                        account acc = (account)Session["acc"];
                        <ul class="nav navbar-nav menu_nav ml-auto">
                            <li class="nav-item" id="homeMenu"><a class="nav-link" href="@Url.Action("Index", "Home")">@ShoesProject.Resources.Data.Home</a></li>
                            <li class="nav-item" id="productMenu"><a class="nav-link" href="@Url.Action("SanPham", "SanPham")">@ShoesProject.Resources.Data.Product</a></li>
                            <li class="nav-item" id="manageAccountMenu"><a class="nav-link" href="/ManageAccount/Index">@ShoesProject.Resources.Data.Hi, @(acc.fullname)</a></li>
                            <li class="nav-item"><a class="nav-link" href="/Login/SignOut">@ShoesProject.Resources.Data.Logout</a></li>
                        </ul>
                        <ul class="nav navbar-nav">
                            @using (Html.BeginForm())
                            {
                                <select id="lang" name="lang" onchange="this.form.submit()">
                                    <option value="en-US" @(Culture != "vi-VN" ? "selected" : "")>English</option>
                                    <option value="vi-VN" @(Culture == "vi-VN" ? "selected" : "")>Tiếng Việt</option>
                                </select>
                            }


                        </ul>
                    }
                    @* <ul class="nav navbar-nav navbar-right">
            <li class="nav-item">
                <button class="search"><span class="lnr lnr-magnifier" id="search"></span></button>
            </li>
        </ul>*@
                </div>
                <div class="header-left" style="margin-left:20px">

                    @Html.Action("GioHangPartial", "GioHang")@*Tách icon giỏ hàng sang partial view*@

                    <div class="clearfix"> </div>
                </div>

            </div>
        </nav>
    </div>
    @*
        <div class="search_input" id="search_input_box">
            <div class="container">
                <form class="d-flex justify-content-between">
                    <input type="text" class="form-control" id="search_input" placeholder="Search Here">
                    <button type="submit" class="btn"></button>
                    <span class="lnr lnr-cross" id="close_search" title="Close Search"></span>
                </form>
            </div>
        </div>*@
</header>
<script>
   /* function changeLanguage() {
        var language = $("#lang").val();
        $.ajax({
           type: "GET",
            contentType: "application/json",
            data: { lang: language },
            url: "/Language/ChangeLanguage",
            cache: false,
            timeout: 600000,
            success: function (data) {
                location.reload();

            },
            error: function (e) {

                alert("Have error!");

            }
        });

    }*/

</script>

<!-- End Header Area -->